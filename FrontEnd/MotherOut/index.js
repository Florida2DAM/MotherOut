/**
 * @format
 */

import {AppRegistry} from 'react-native';
import TasksEditiing from './components/screens/TasksEditiing';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TasksEditiing);
